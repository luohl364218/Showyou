<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 实心-->
    <solid android:color="@android:color/white"/>

    <!-- 边框-->
    <stroke
        android:color="@color/alpha_30_black"
        android:width="1dp"/>

    <!-- 圆角-->
    <corners
        android:radius="10dp"/>

    <padding
        android:bottom="2dp"
        android:top="2dp"
        android:left="10dp"
        android:right="10dp"
        />

</shape>